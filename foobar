#include <iostream>
#include<string>
using namespace std;
int printNos(int n) {
    int counter = 1;
    while (counter <= n) {
        if ((counter % 3 == 0) && (counter % 5 == 0))
            cout << "FooBar ";
        else if (counter % 3 == 0)
            cout << "Foo ";
        else if (counter % 5 == 0)
            cout << "Bar ";
        else
            cout << counter << " ";
        counter++;
    }
    return 1;
}
int main() {
    printNos(100);
    return 0;
}
